{
  "name": "salonmind-backend",
  "version": "1.0.0",
  "description": "SalonMind Backend API with Express and MongoDB",
  "main": "src/server.js",
  "scripts": {
    "dev": "nodemon src/server.js",
    "start": "node src/server.js",
    "prod": "NODE_ENV=production node src/server.js",
    "seed": "node src/scripts/seed.js",
    "logs": "tail -f /tmp/salonmind-backend.log",
    "logs:clean": "rm -f /tmp/salonmind-backend.log",
    "health": "curl -s http://localhost:5000/health | jq",
    "lint": "echo 'Linting not configured yet'",
    "format": "echo 'Formatting not configured yet'",
    "test": "echo 'Tests not configured yet'",
    "test:unit": "echo 'Unit tests not configured yet'",
    "test:integration": "echo 'Integration tests not configured yet'"
  },
  "keywords": [
    "salon",
    "management",
    "api"
  ],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "express": "^4.18.2",
    "mongoose": "^8.0.0",
    "dotenv": "^16.3.1",
    "bcryptjs": "^2.4.3",
    "jsonwebtoken": "^9.0.2",
    "cors": "^2.8.5",
    "express-validator": "^7.0.1",
    "express-rate-limit": "^7.1.5",
    "morgan": "^1.10.0",
    "winston": "^3.11.0"
  },
  "devDependencies": {
    "nodemon": "^3.0.2"
  }
}
