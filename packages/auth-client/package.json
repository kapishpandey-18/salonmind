{
  "name": "@salonmind/auth-client",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "main": "src/index.ts",
  "module": "src/index.ts",
  "types": "src/index.ts",
  "scripts": {
    "dev": "tsc -w --noEmit",
    "build": "tsc --emitDeclarationOnly",
    "lint": "echo 'auth-client lint not configured'",
    "test": "vitest run",
    "e2e": "echo 'auth-client e2e not applicable'"
  },
  "dependencies": {
    "@capacitor/preferences": "^6.0.2",
    "axios": "^1.7.9",
    "buffer": "^6.0.3"
  },
  "devDependencies": {
    "typescript": "^5.6.3",
    "vitest": "^2.1.4"
  }
}
